
export DOCKER_HOST=tcp://demo.xt.msg.team:443/ase-docker/
export DOCKER_TLS=1
export DOCKER_CONFIG=$(pwd)/docker.d

#echo -n "admin:admin" | openssl base64 -e
#docker run --rm -it xmartlabs/htpasswd -B -b admin admin

